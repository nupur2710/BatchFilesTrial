define(["source/tei/authoring/engine-templates/base-classes/js/views/tabs/feedback-scoring-base"], function(ScoreFeedbackBase) {
    "use strict";

    return ScoreFeedbackBase.extend({}); //extended the base class which has common score feedback component, for future over riding of base class functions
});
